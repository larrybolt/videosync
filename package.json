{
  "name": "VideoSync",
  "description": "Watch video in sync with other people.",
  "version": "0.0.2",
  "private": true,
  "scripts": {
    "start": "node app",
    "up"   : "npm install && node app"
  },
  "dependencies": {
    "express": "3.x",
    "jade": "*",
    "less-middleware": "*",
    "socket.io": "*"
  }
}
